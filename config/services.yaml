# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
#            - '../src/Service/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
#    App\Grant\FakeGrant: ~
#    Psr\Http\Message\ServerRequestInterface: ~
#    Psr\Http\Message\ServerRequestInterface:
#        factory: 'App\Controller\AuthController:accessToken'
#        arguments: ['@=service("request_stack").getMasterRequest()']


#    TokenService:
#        class: App\Service\TokenService
#        arguments: [
#            '@server',
#            '@httpMessageFactory',
#            '@httpFoundationFactory',
#            '@responseFactory',
#            '@eventDispatcher'
#        ]

#    _instanceof:
#        Symfony\Bridge\PsrHttpMessage\HttpMessageFactoryInterface:
#            tags: ['league.oauth2_server.factory.psr_http']


#    Symfony\Bridge\PsrHttpMessage\HttpMessageFactoryInterface: ~
#    Symfony\Bridge\PsrHttpMessage\Factory: ~

#    App\Service\TokenService: ~

#    App\Service\TokenService:
#        autowire: true



    #

#    Symfony\Bridge\PsrHttpMessage\HttpMessageFactoryInterface $httpMessageFactory: '@App\Service\TokenService'


#    App\Service\TokenService: ~
#    league.oauth2_server.factory.psr_http:
#        class: Symfony\Bridge\PsrHttpMessage\HttpMessageFactoryInterface
#    Symfony\Bridge\PsrHttpMessage\HttpMessageFactoryInterface: ~
#    Symfony\Bridge\PsrHttpMessage\HttpMessageFactoryInterface: '@league.oauth2_server.factory.psr_http'

#    App\Service\:
#        resource: '../src/Service/'
#        autowire: true
#        public: true

#    Symfony\Bridge\PsrHttpMessage\HttpFoundationFactoryInterface: ~
#
#    App\Service\TokenService: '@Symfony\Bridge\PsrHttpMessage\HttpMessageFactoryInterface'
#    App\Service\TokenService:
#        tags:
##            - { name: 'Symfony\Bridge\PsrHttpMessage\Factory' }
#            - { name: 'Symfony\Bridge\PsrHttpMessage\HttpMessageFactoryInterface' }
#        factory: 'Symfony\Bridge\PsrHttpMessage\Factory:createRequest'
#        factory: 'Symfony\Bridge\PsrHttpMessage\HttpMessageFactoryInterface:createRequest'
#        factory: 'Symfony\Bridge\PsrHttpMessage\Factory\HttpMessageFactoryInterface:createRequest'
#        arguments: ['@=service("request_stack").getMasterRequest()']